import React, { useState } from 'react'

const Multipalreturn = () => {

const [users, setUsers] = useState([]);
const [isLoding, setIsLoding] = useState(true);
const [error, setError] = useState(null);

  return (
    <div>
      
    </div>
  )
}

export default Multipalreturn;
